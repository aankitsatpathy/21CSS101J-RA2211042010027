#include <stdio.h>

int main()
{
    int t,a,r=0;
    printf("Enter a number : ");
    scanf("%d",&a);
    t=a;
    for(;a>0;a/=10)
    r=r*10+a%10;
    if(t==r)
    printf("Palindrome Number");
    else
    printf("Not A Palindrome Number");
    
}
